
import { enqueue } from '../../lib/utils';

describe("utils::enqueue", function () {
  it('exists', function () {
    enqueue.should.be.a('function');
  });
  it('executes one job after another');
});
